import { StyleSheet} from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    marginVertical: 50,
    marginHorizontal: 50,
  },
  button: {
    paddingVertical: 10,
  },
});

export default styles;
